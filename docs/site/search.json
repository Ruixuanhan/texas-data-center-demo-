{"config":{"lang":["en"],"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Project Radar","text":"<p>A live situation screen for U.S. energy capital projects — Texas first. Interconnection queues, utility-commission dockets, environmental permits, county agendas, and press are stitched into one addictive, self-updating map.</p>","path":["Project Radar"],"tags":[]},{"location":"#what-it-does","level":2,"title":"What it does","text":"<ul> <li>Live map + signal wire — every new filing appears in the feed within seconds and pulses its project on the map. Realtime via Supabase; the UI degrades invisibly to 5-second polling if the channel drops.</li> <li>Entity resolution, made visible — a project's dossier shows its canonical name and every resolved alias (LLC, queue name, permit name) with per-alias confidence.</li> <li>Stage inference, made visible — the FEL ladder (concept → FEL-1 → FEL-2/pre-FEED → FEED → interconnection agreement → FID → construction → COD) with a confidence meter and the filings that justify each call.</li> <li>One-screen story — click any project and its entire cross-source history renders on a single screen with provenance links.</li> </ul>","path":["Project Radar"],"tags":[]},{"location":"#architecture","level":2,"title":"Architecture","text":"Layer Choice Frontend Next.js 15 (App Router), deployed on Vercel Map MapLibre GL + deck.gl overlays (no vendor token) Data Supabase Postgres + Realtime — see Data contract Tokens <code>web/DESIGN.md</code> → CSS custom properties → Tailwind (see Design system) Components Storybook + Chromatic visual testing; Playwright smoke tests Secrets Doppler (<code>energy-hackathon</code>)","path":["Project Radar"],"tags":[]},{"location":"#running-it","level":2,"title":"Running it","text":"<pre><code>cd web\ndoppler run -- bun dev                          # app on :3000\ndoppler run -- bun scripts/simulate-feed.ts --seed   # one-time backfill\ndoppler run -- bun scripts/simulate-feed.ts --live   # demo heartbeat\nbun run storybook                                # component library on :6006\n</code></pre>","path":["Project Radar"],"tags":[]},{"location":"#what-wed-build-with-another-week","level":2,"title":"What we'd build with another week","text":"<p>National coverage beyond ERCOT; live scrapers on cron for every source; LLM stage-inference with eval harness; alerting (stage-change push/email); the Track-2 join — speakers ↔ projects in one graph.</p>","path":["Project Radar"],"tags":[]},{"location":"data-contract/","level":1,"title":"Data contract","text":"<p>Frozen at kickoff (<code>supabase/migrations/0001_init.sql</code>). Columns are add-nullable-only; never rename. Writers use the service role; the frontend reads with the anon key (RLS <code>SELECT</code> policies).</p>","path":["Data contract"],"tags":[]},{"location":"data-contract/#tables","level":2,"title":"Tables","text":"","path":["Data contract"],"tags":[]},{"location":"data-contract/#projects-one-row-per-resolved-entity","level":3,"title":"<code>projects</code> — one row per resolved entity","text":"<p>Upsert key: <code>slug</code>. Carries denormalized <code>current_stage</code> + <code>stage_confidence</code> for map styling, plus <code>headline</code> and <code>last_activity</code> for tooltips and sorting.</p>","path":["Data contract"],"tags":[]},{"location":"data-contract/#project_aliases-entity-resolution-evidence","level":3,"title":"<code>project_aliases</code> — entity-resolution evidence","text":"<p><code>alias</code>, <code>alias_type</code> (<code>llc | permit_name | queue_name | docket_name | press_name</code>), <code>confidence</code>. Renders as the dossier's alias cluster.</p>","path":["Data contract"],"tags":[]},{"location":"data-contract/#source_events-the-live-feed-and-dossier-timeline","level":3,"title":"<code>source_events</code> — the live feed and dossier timeline","text":"<p><code>project_id</code> is nullable — unattributed signals are a feature (the \"resolution pending\" lane). <code>source</code> enum covers <code>ercot_gis, ercot_rioo, ercot_mora, puct, ferc, tceq, rrc, county, municipal, press, earnings, oem_epc, cleanview, simulator, manual</code>. <code>severity ∈ low | notable | major</code> drives feed treatment. Every event should carry a <code>url</code> (provenance).</p>","path":["Data contract"],"tags":[]},{"location":"data-contract/#stage_history-stage-inference-with-provenance","level":3,"title":"<code>stage_history</code> — stage inference with provenance","text":"<p><code>stage</code>, <code>confidence</code> (0–1), <code>rationale</code>, <code>evidence_event_ids[]</code>, <code>inferred_by ∈ rules | llm | human</code>.</p>","path":["Data contract"],"tags":[]},{"location":"data-contract/#realtime-channels","level":2,"title":"Realtime channels","text":"<p>The frontend subscribes to <code>postgres_changes</code>:</p> <ul> <li><code>source_events</code> INSERT → feed item + map pulse</li> <li><code>stage_history</code> INSERT → stage-change chyron</li> <li><code>projects</code> UPDATE → marker restyle + KPI tick</li> </ul> <p>RLS must allow anon <code>SELECT</code> on all four tables or realtime delivers nothing — this is enabled in the migration.</p>","path":["Data contract"],"tags":[]},{"location":"data-contract/#rules-of-engagement","level":2,"title":"Rules of engagement","text":"<ol> <li>Ingestion INSERTs <code>source_events</code> / <code>stage_history</code> / <code>project_aliases</code>; UPSERTs <code>projects</code> on <code>slug</code>.</li> <li>Add columns as nullable; never rename or repurpose.</li> <li>Every event carries a source <code>url</code>.</li> <li>Stage values come from the shared enum: <code>concept, fel1, fel2, feed, ia, fid, construction, cod, operational, canceled, unknown</code>.</li> </ol>","path":["Data contract"],"tags":[]},{"location":"design-system/","level":1,"title":"Design system","text":"<p>Tokens live in code — <code>web/DESIGN.md</code> (Open Design convention) is the source of truth, mirrored as CSS custom properties in <code>web/app/globals.css</code> and consumed by Tailwind, the app, and Storybook. Figma sync is a post-demo step (tokens flow code → Figma, not the reverse, during the hackathon).</p> <p>This page is finalized after the direction bake-off — the chosen direction's palette, type system, and motion vocabulary land here.</p>","path":["Design system"],"tags":[]},{"location":"design-system/#principles","level":2,"title":"Principles","text":"<ol> <li>Data visualizer first, not a SaaS. The map is the hero; chrome exists to frame signal.</li> <li>Color only where it means something — stage ramp, severity, liveness. Everything else is ink.</li> <li>Numbers are monospaced and tabular. Timestamps are relative and alive.</li> <li>Motion = information. A pulse is a new filing; a wipe is a stage change. No decorative animation.</li> </ol>","path":["Design system"],"tags":[]},{"location":"design-system/#component-inventory-storybook","level":2,"title":"Component inventory (Storybook)","text":"<p>Foundations (tokens) · StageBadge · SeverityTag · SourceChip · ConfidenceMeter · RelativeTime · KpiStat · EventCard (feed item) · StageLadder · AliasCluster · DossierTimeline · PulseMarker (map dot states).</p> <p>Run locally: <code>cd web &amp;&amp; bun run storybook</code>. Visual regression: Chromatic (<code>bunx chromatic</code>).</p>","path":["Design system"],"tags":[]}]}